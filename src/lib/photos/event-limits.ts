/** Límite operativo para eventos piloto / plan Pro. */
export const MAX_PHOTOS_PER_EVENT = 5000;

/** Fotos por archivo ZIP en exportación masiva (control de memoria en serverless). */
export const EXPORT_PHOTOS_PER_PART = 50;
